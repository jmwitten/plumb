# Task CPGCORE — STEPDOC/CPG v1-core semantic core (event graph, axis-3, rung wording, corpus)

**Worktree:** `~/Code/detailgen-stepdoc-core` (branch `sdd/stepdoc-core`; SEQSCHEMA's loader plumbing is already committed on it — read `.superpowers/sdd/task-seqschema-report.md` first).
**Import discipline (non-negotiable):** `mkdir -p .shim && ln -sfn "$PWD/src" .shim/detailgen; export PYTHONPATH="$PWD/.shim"`; verify `python -c "import detailgen; print(detailgen.__file__)"` prints the WORKTREE path before trusting ANY run. Python: `~/Code/construction-detail-generator/.venv/bin/python`. Single-module dev runs may use `-p no:xdist`; the controller runs the final full gate.

**Binding sources (read in this order):**
1. `.superpowers/sdd/stepdoc-cpg-design.md` — the WHOLE doc. §Owner sign-off amendments are BINDING; amendment 1 defines v1-core exactly.
2. `.superpowers/sdd/review-stepdoc-design.md` — the reviewer's live constructions your tests must pin (F-5 bolt-stack self-loops, R-2 hanger multi-stack mapping + its cycling counterexample, F-7 party delta, F-11 wedge fact, Q9 four-property probe form).
3. `.superpowers/sdd/task-install-axes-report.md` — the axis-2 machinery you are extending; the re-pin inventory pattern.

## Scope = v1-core ONLY (amendment 1). Do NOT build: staging/subassemblies/`assembly:`/`in_situ`, `after:` point constraints, cure/process events, viewer slider, step PNGs, per-step view coverage, evidence-graph projection, caddy prose retirement. Those are later increments.

Deliverables:

1. **Event graph (CPG assembly slice).** Nodes: `place(part)` / `drive(connection, role_group)` (content-keyed identity per §2; `bond` = drive with empty hardware set; NO `process`/`join` kinds in v1-core — leave the open-tag headroom, don't build them). Edge families in v1-core: `technique_default` (lifted), `structural_necessity` (derived), `authored_sequence` (declared, from SEQSCHEMA's parsed stages: a stage orders its connections' drive events and its parts' place events; stage k's events precede stage k+1's). Every edge provenance-stamped with its family + source claim.
   - **BOTH lift rules, each pinned by test with the reviewer's construction:** (a) same-event edges are order-vacuous and DROP (the platform's 40 intra-bolt-stack edges — without it every bolted detail fails at load); (b) multi-stack hardware maps to the drive event its OWN type edges place it at-or-before (the FaceMountHanger's hanger → header-side drive; the test must pin that the alternative hung-side mapping would cycle — encode the reviewer's counterexample as the test's rationale and assert the shipped mapping loads cycle-free with the expected collapsed edges).
   - **Structural necessity:** `place(member) → drive(conn, g)` per member, EXCEPT where a declared technique path orders the opposite way (hanger header screws before hung member); residual contradiction = loud cycle error. Every derived edge points INTO a drive event — preserve this invariant; it is why §4.3's rung ceiling holds.
   - **Merged cycle check:** one check over the whole event graph (extend/generalize `_check_install_order`, connection.py:611), teaching error naming the conflicting edges AND their provenance families.
2. **Axis-3 reclassification (§4.1/§4.2).** Same hit sets, reclassified against the event graph (accrete-at-final-pose assumption, stated in the verdicts' disclosure): occupant provably ≤ fastener ⇒ FAIL naming occupant + order facts + provenance; fastener provably < occupant ⇒ disclosed clear (generalized `_later_note`); neither ⇒ blocking `UNKNOWN — build order underdetermined` naming occupant AND the missing order fact. `_Scope.installed_after`'s own-connection closure becomes a projection of the ONE event graph (single order truth).
   - **Party-rule delta (F-7), deliberate:** same-connection MEMBER occupants keep FAIL via structural necessity (provenance printed); unordered cross-group HARDWARE re-pins from FAIL to the named UNKNOWN. Re-pin the CAT-E-lineage fixtures in `tests/test_install_axes.py` to the new truth — re-derived deliberately, never silently weakened; no shipped detail changes verdict (reviewer-verified; assert that where the corpus pins it).
3. **Rung wording (§4.3), grep-enforceable:** every axis-3 clear reads **"geometry proven at the DECLARED build order"** with every deciding declaration + provenance printed inline and an explicit "declared, not sequence-proven". The string `SEQUENCE-PROVEN` is claimed NOWHERE (add a guard test greping the source/verdict surface). Declared-trust visibility is a product requirement (amendment 3): any summary surface counting a declared-order clear as resolved carries the declared marker — "resolved" never reads as "proved".
4. **Epistemic-contract table (amendment 2):** a rendered table near the top of the axis-3 disclosure surface (the `render_install_disclosures_md` section that reaches validation_report.md + the HTML build doc): which order-fact families are DERIVED (structural necessity, technique lifts), DECLARED (authored `sequence:` stages — provenance + why), UNKNOWN (undeclared context bodies, cross-fragment order, insertability). Static content, auditable at a glance.
5. **Platform corpus row (§4.4).** Author the `sequence:` block in `details/platform.spec.yaml`: toe-screw connections staged before the leg-bolt connections, `why:` citing the ToeScrewed wedge fact (the end joist drops into the ~3.5 in gap BETWEEN the bolts — bolts-first is unbuildable under the model). EXACTLY the 2 top toe-screw UNKNOWNs flip to PASS, double-qualified: (a) declared build strategy, never derived, not sequence-proven (amendment 4); (b) the joist-insertion disclosing sentence — insertion travel itself is not analyzed (P1). No other verdict moves anywhere in the corpus (pin that).
6. **Re-word the 17 remaining install-order UNKNOWN texts** (caddy 8, frame 8, site rod-vs-rung 1): they cite "before a construction process graph exists (axis 3)" — false once this lands. New wording per §4.1 names the occupant and the missing order fact. CONSTRAINT: teaching text may only name authoring surfaces that EXIST in v1-core (`sequence:` stages, technique edges); staging declarations may be named only as an explicitly-future mechanism, never as authorable today. The site rod-vs-rung reason names BOTH missing mechanisms (cross-fragment order — CPG v2 — AND insertability — P1) per §3.2. Text-only: all 17 verdicts stay blocking UNKNOWNs.
7. **CAT-I, both halves** (§7): (a) test-local platform variant authoring the OPPOSITE order (bolts before toes) ⇒ the 2 UNKNOWNs flip to FAIL by mechanism (UNKNOWN→FAIL both directions proven; authoring is falsifiable, not a waiver channel); (b) load-time half: a stage contradicting the hanger's technique edges ⇒ cycle error naming both claims and their provenance families.
8. **Reversion probes (Q9 form):** remove the platform's `sequence:` block (spec-TEXT mutation, shipped spec untouched — the install-sweep mutation pattern) ⇒ the 2 blocking UNKNOWNs RETURN, asserting all four properties: verdict class UNKNOWN, blocking, occupant named (the bolts/nuts), missing order fact named.
9. **ONE derived plain-text reader surface (§5.1/§5.2 minus views).** Kahn linearization of the event graph (ties: declared stage order, then connection declaration order, then part authored id — byte-stable), reader-step grouping (one step per authored stage where stages exist; else per connection install unit; `place` folds into the first consuming connection's step), and a derived "Build Sequence" text section on the per-detail reader surfaces (validation_report.md lifecycle section + the HTML build document, mirroring how `render_install_disclosures_md` reaches both). Every step whose position rests on an authored stage prints the claim + why inline. NOTHING hand-typed. **Vocabulary (amendment 5, BINDING): "stage" = authored grouping, "step" = reader presentation unit — distinct types/field names; blurring unrepresentable.**
10. **Re-pins** to the new truth across the suite (the axes report's re-pin inventory is the pattern): platform blocking set {capacity×3, install_access×2} → {capacity×3}; frozen truth refreeze where justified (findings_fp/content_fp only unless geometry actually moved — it must NOT); baselines/goldens; caddy/frame/site e2e text re-pins (verdicts unchanged). Every re-pin deliberate and to the new truth — nothing silenced. If a golden regen is pure mechanical churn, do it and say so in the report.

## Honesty rules (binding)
- Never declare/author an order fact to silence a verdict; the platform stage's why is a real technique defense (F-11).
- Honest FAIL/UNKNOWN over fake PASS; never weaken a check.
- Underdetermined ≠ resolved: no default presence assumption in either direction.
- Insertability not analyzed at any rung — said in every verdict class that leans declared.
- Doc-view PNGs are existence-gated (`_ensure_views` fills missing only) — this arc changes NO geometry, so no PNG should need re-render; if any geometry hash moves, STOP and report.

## Gates & report
- Suite green from the worktree before you finish (`pytest -n auto -q`, shimmed, import path verified at run start). An intermediate red is fine if every failure is triaged into deliberate re-pins.
- Report: `.superpowers/sdd/task-cpgcore-report.md` — mechanisms + file/line anchors, per-deliverable status, the platform verdict texts verbatim, re-pin inventory, residuals, suite counts. Write the report file even if you stop early — disk is the handoff channel.
- Commit(s) on `sdd/stepdoc-core`, messages starting `cpgcore:`.
- If this brief conflicts with source reality or the design, STOP on that item, record the conflict in the report, and continue with the rest.
