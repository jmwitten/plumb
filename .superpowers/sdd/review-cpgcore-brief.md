# Review brief — CPGCORE (STEPDOC/CPG v1-core), branch `sdd/stepdoc-core`

**Stance:** fresh adversary. You have no stake in this branch. Every geometric/mechanism claim that can be checked by running code MUST be checked by running code from the worktree (`~/Code/detailgen-stepdoc-core`; shim discipline: `mkdir -p .shim && ln -sfn "$PWD/src" .shim/detailgen; PYTHONPATH="$PWD/.shim"`, verify `import detailgen` prints the WORKTREE path first; python `~/Code/construction-detail-generator/.venv/bin/python`).

**Target:** the full diff master @c4692f5 → branch tip (commits `09a116f` seqschema + `00f9022`..`b9c3029` cpgcore). Reports: `.superpowers/sdd/task-seqschema-report.md`, `task-cpgcore-report.md`.

**Binding acceptance surface:**
- `.superpowers/sdd/stepdoc-cpg-design.md` — §Owner sign-off amendments are BINDING; amendment 1 defines v1-core scope exactly (staging/cure/slider/PNGs/evidence-projection must NOT have landed).
- `.superpowers/sdd/review-stepdoc-design.md` — your predecessor's constructions (F-5, R-2 incl. the cycling counterexample, F-7, F-11, Q9's four-property probe form) must be pinned by tests that would actually catch their regressions.
- `.superpowers/sdd/task-install-axes-report.md` — the axis-2 substrate; nothing there may have been weakened.

**Highest-priority probes (in order):**
1. **The THIRD lift rule (mid-event member interleave) — NOT in the reviewed design.** The implementer discovered the shipped ThreadedRodEpoxyAnchor threads hardware through a member and shipped a new rule: both directions between one connection's own drive event and a member's place event drop as intra-event order; structural necessity keeps the member PRESENT. Attack it: is dropping BOTH directions sound, or does it discard a real order fact in some construction (multi-connection interleaves? a member that genuinely must arrive after another connection's drive)? Is the rule scoped to ONE connection's own lifts, and can it mask a genuine cross-connection cycle? Construct counterexamples and run them.
2. **Axis-3 §4.1 semantics on the ONE graph.** Can you construct a false PASS (an occupant read "provably later" that a valid linearization puts earlier)? Verify reachability direction, the accretion disclosure, and that `_Scope`'s old own-connection closure is genuinely gone (single order truth) without changing any verdict the design says must not move.
3. **Corpus truth, live.** Compile platform, caddy, frame, rock_anchor, site: exactly the 2 platform top-toe UNKNOWNs flip to PASS (double-qualified: declared-not-derived AND insertion-P1); caddy 8 + frame 8 + site rod-vs-rung stay blocking UNKNOWNs with the §4.1 re-words (occupant named, missing order fact named, staging named only as FUTURE, site names BOTH gaps); no other verdict moves corpus-wide; the old "before a construction process graph exists" sentence is gone everywhere.
4. **CAT-I both halves + Q9 probes** — run them; check the probes assert all four Q9 properties and would catch a weaker-UNKNOWN regression.
5. **Rung/marker discipline.** SEQUENCE-PROVEN claimed nowhere (guards real, not vacuous); declared-trust marker on every summary surface that counts declared-order clears; amendment 5 stage/step vocabulary discipline in the new code (`ReaderStep` vs `AuthoredStage`/`ResolvedStage`); the flagged pre-existing `FastenerInstallation.stage` collision is a known residual, not a finding.
6. **Suite arithmetic.** Controller-verified master baseline 1116/3/1; final gate 1166/3/1 (+50). The reports' itemized growth sums to +49 (28+1 seqschema, 20 cpgcore). Find the unaccounted test — confirm it is legitimate, not an accidental behavior change flipping something.
7. **Re-pin honesty sweep.** Diff every changed test/golden/baseline/frozen-truth: each re-pin to the NEW truth with a reason, nothing silenced or weakened; platform frozen-truth refreeze justified (geometry fingerprint byte-identical); reverted churn genuinely reverted.

**Verdict vocabulary:** MERGE / MERGE-WITH-NITS (nits listed, none blocking) / FIX-FIRST (named blocking findings with constructions) / REJECT. Write findings F-1.. with severity, each with the code/run evidence. Output: `.superpowers/sdd/review-cpgcore.md`. Write the file even if you stop early. Do NOT edit source or tests — review only. Do NOT run the full suite (the controller owns the merge gate); scoped runs are yours.
