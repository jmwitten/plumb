# Platform instruction precision implementation plan

1. Add one focused birdhouse regression to the existing instruction-panel
   test module: scoped panel size, modeled layout text, service head behavior,
   and final state-derived closeout.
2. Extend stage schema, loader, compiler, site replay, and serializer with the
   optional `setup` and `check` strings.
3. Add `seated` to the installation vocabulary, make it the ordinary straight
   screw default, and keep service-panel screws explicitly `proud`.
4. Add the shared model-derived `FastenerLayout` projection and render its
   concise location lines in the manual.
5. Compose each fastener panel as setup, one generated installation action,
   and check; simplify hardware rows.
6. Split the birdhouse floor/roof stages, author their tight setup/check facts,
   include the cleat in the bench unit, and remove the duplicated closeout
   count.
7. Run the focused regression, birdhouse inner/release gates, platform
   integration gate, package build, artifact verification, and visual review
   of every generated diagram. Regenerate and sync the governed vault package.

